services:
  - type: web
    name: nexus-gold-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    routes:
      - type: rewrite
        source: /*
        destination: /
